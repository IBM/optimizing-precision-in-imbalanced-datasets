# @package _global_
dataloader:
    name: spectrometer

    # PRNG Settings
    seed: 1234
    enable_seed: true

    # Dataset File Location
    data_path: '../data/spectrometer/lrs.data'

    # Cross Validation Parameter
    crossval:
        folds: 10
        stratified: false
        shuffle: true
        random_state: 1234
